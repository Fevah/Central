<dxc:DXWindow x:Class="Central.Module.Networking.Governance.SubmitChangeSetDialog"
              xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
              xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
              xmlns:dxc="http://schemas.devexpress.com/winfx/2008/xaml/core"
              xmlns:dxe="http://schemas.devexpress.com/winfx/2008/xaml/editors"
              Title="Submit Change Set"
              Width="460" Height="240"
              WindowStartupLocation="CenterOwner"
              ResizeMode="NoResize">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" x:Name="HeaderLabel"
                   FontWeight="SemiBold" Foreground="#D0D0D0"
                   Margin="0,0,0,10"/>

        <TextBlock Grid.Row="1" Text="Required approvals" Margin="0,0,0,4" Foreground="#B0B0B0"/>
        <dxe:SpinEdit Grid.Row="2" x:Name="ApprovalsEdit"
                      Value="1" MinValue="1" MaxValue="20"
                      IsFloatValue="False"
                      Margin="0,0,0,10"/>

        <TextBlock Grid.Row="3" Margin="0,0,0,10" TextWrapping="Wrap"
                   Foreground="#909090" FontSize="11"
                   Text="Once submitted, the Set becomes read-only until approvers record decisions. Items can only be added while the Set is in Draft."/>

        <TextBlock Grid.Row="4" x:Name="ErrorLabel" Foreground="#E06464"
                   TextWrapping="Wrap" Margin="0,0,0,8" VerticalAlignment="Top"
                   Visibility="Collapsed"/>

        <StackPanel Grid.Row="5" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Cancel" Width="90" Margin="0,0,8,0"
                    Click="OnCancel" IsCancel="True"/>
            <Button x:Name="OkButton" Content="Submit" Width="100"
                    Click="OnOk" IsDefault="True"/>
        </StackPanel>
    </Grid>
</dxc:DXWindow>
