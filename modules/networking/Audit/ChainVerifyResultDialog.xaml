<dxc:DXWindow x:Class="Central.Module.Networking.Audit.ChainVerifyResultDialog"
              xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
              xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
              xmlns:dxc="http://schemas.devexpress.com/winfx/2008/xaml/core"
              xmlns:dxg="http://schemas.devexpress.com/winfx/2008/xaml/grid"
              Title="Audit Chain Verification"
              Width="820" Height="480"
              WindowStartupLocation="CenterOwner"
              ResizeMode="CanResize"
              MinWidth="560" MinHeight="360">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" x:Name="HeadlineLabel"
                   FontWeight="SemiBold" FontSize="16"
                   Margin="0,0,0,6"/>

        <TextBlock Grid.Row="1" x:Name="DetailsLabel"
                   TextWrapping="Wrap" Foreground="#B0B0B0"
                   Margin="0,0,0,10"/>

        <dxg:GridControl Grid.Row="2" x:Name="MismatchesGrid"
                         AutoGenerateColumns="None" SelectionMode="Row"
                         ShowBorder="True">
            <dxg:GridControl.View>
                <dxg:TableView AutoWidth="True" ShowIndicator="False"
                               AllowEditing="False"/>
            </dxg:GridControl.View>
            <dxg:GridControl.Columns>
                <dxg:GridColumn FieldName="SequenceId"   Header="Seq"           Width="60"/>
                <dxg:GridColumn FieldName="Id"           Header="Audit Row ID"  Width="260"/>
                <dxg:GridColumn FieldName="Reason"       Header="Reason"        Width="*"/>
                <dxg:GridColumn FieldName="ExpectedHash" Header="Expected"      Width="130"/>
                <dxg:GridColumn FieldName="StoredHash"   Header="Stored"        Width="130"/>
            </dxg:GridControl.Columns>
        </dxg:GridControl>

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right"
                    Margin="0,8,0,0">
            <Button Content="Close" Width="90" Click="OnClose" IsDefault="True" IsCancel="True"/>
        </StackPanel>
    </Grid>
</dxc:DXWindow>
