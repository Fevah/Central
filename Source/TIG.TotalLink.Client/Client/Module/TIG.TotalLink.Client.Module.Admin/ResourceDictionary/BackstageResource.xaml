<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:dxr="http://schemas.devexpress.com/winfx/2008/xaml/ribbon"
                    xmlns:item="clr-namespace:TIG.TotalLink.Client.Module.Admin.ViewModel.Backstage.Item"
                    xmlns:templateSelector="clr-namespace:TIG.TotalLink.Client.Module.Admin.TemplateSelector">

    <templateSelector:BackstageItemTemplateSelector x:Key="BackstageItemTemplateSelector" />
    
    <!-- Backstage Button Item template -->
    <DataTemplate DataType="{x:Type item:BackstageButtonItemViewModel}">
        <ContentControl>
            <dxr:BackstageButtonItem Content="{Binding Name}"
                                     Command="{Binding Command}"
                                     CommandParameter="{Binding CommandParameter}"
                                     />
            <!--Glyph="{Binding SmallGlyph}"
                    LargeGlyph="{Binding LargeGlyph}"-->
        </ContentControl>
    </DataTemplate>


    <!-- Backstage Tab Item template -->
    <DataTemplate DataType="{x:Type item:BackstageTabItemViewModel}">
        <ContentControl>
            <dxr:BackstageTabItem Content="{Binding Name}"
                                  ControlPane="{Binding Content}"/>
        </ContentControl>
    </DataTemplate>
   
</ResourceDictionary>