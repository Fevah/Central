<Window x:Class="Central.Desktop.SplashWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        WindowStartupLocation="CenterScreen"
        Width="460" Height="280" ResizeMode="NoResize"
        ShowInTaskbar="False" Topmost="True">
    <Border Background="#1E1E2E" CornerRadius="12" BorderBrush="#3B82F6" BorderThickness="1"
            Effect="{x:Null}">
        <Grid Margin="32">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0" Text="Central" FontSize="32" FontWeight="Bold"
                       Foreground="White" HorizontalAlignment="Center" Margin="0,0,0,4" />
            <TextBlock Grid.Row="1" Text="Enterprise Infrastructure Platform" FontSize="12"
                       Foreground="#9CA3AF" HorizontalAlignment="Center" Margin="0,0,0,24" />

            <TextBlock x:Name="StatusText" Grid.Row="2" Text="Starting..."
                       Foreground="#D1D5DB" FontSize="12" HorizontalAlignment="Center"
                       VerticalAlignment="Center" />

            <ProgressBar x:Name="ProgressBar" Grid.Row="3"
                         Height="4" Margin="0,16,0,8"
                         Background="#374151" Foreground="#3B82F6"
                         Minimum="0" Maximum="100" Value="0" />

            <TextBlock Grid.Row="4" Text="v5.0 · .NET 10 · DevExpress 25.2"
                       Foreground="#6B7280" FontSize="10" HorizontalAlignment="Center" />
        </Grid>
    </Border>
</Window>
